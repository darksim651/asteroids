Boot.dev Asteroids course

Requires virtual environment (venv) to run Python 3.13 in the project directory.

Activate this by typing the following in the project's root directory:

source .venv/bin/activate

To add the pygame library as a project dependency:

uv add pygame==2.6.1

Then to run your game, use the following:

uv run main.py
